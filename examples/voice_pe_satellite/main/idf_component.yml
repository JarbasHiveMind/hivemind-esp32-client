dependencies:
  # esp-sr 2.x exposes the current AFE API (afe_config_init / AFE_TYPE_SR /
  # esp_afe_handle_from_config); the 1.x AFE_CONFIG_DEFAULT() API is gone.
  espressif/esp-sr:
    version: "^2.0.0"
  espressif/led_strip:
    version: "^2.5.0"
